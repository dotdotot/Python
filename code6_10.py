# def 함수 선언 및 정의 
def rectArea(w,h):
    # area 변수 선언 및 w * h로 초기화
    area = w*h
    return area

# area 변수 선언 및 rectArea함수에 3을 인수로 넘겨 초기화
area = rectArea(3)

# rectArea 함수에는 매개 변수가 2개이므로 인수를 1개만 넘긴다면 에러가 발생함

